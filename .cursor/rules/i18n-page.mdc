---
description: 前端页面 / 组件国际化强制规范（web-agent / web-main）— 任何用户可见字符串必须走 next-intl `useTranslations` / `getTranslations`，禁止裸字符串。
globs:
  - apps/web-agent/src/app/**/*
  - apps/web-agent/src/components/**/*
  - apps/web-main/src/app/**/*
  - apps/web-main/src/components/**/*
alwaysApply: false
---

# 前端页面国际化规范

适用范围：`apps/web-agent`、`apps/web-main` 两个前端应用的**所有页面和组件**。

> 两个应用都已经接入 next-intl + 翻译 JSON。新增页面 / 组件时**不接 i18n 就是 bug**，等同于半成品。

---

## 强制清单（写完即自检）

每写完一个页面 / 组件文件，按这份清单逐项过一遍：

- [ ] 顶部 `import { useTranslations } from "next-intl";`（客户端组件）或 `import { getTranslations } from "next-intl/server";`（服务端组件）
- [ ] 组件函数内拿到 `t = useTranslations("<namespace>")`，`<namespace>` 用 kebab-case 或与页面同名（如 `"session"` / `"schedule"` / `"home"`）
- [ ] **所有以下位置**的字符串都换成 `t("key")`：
  - JSX 文本节点（`<h1>计划任务</h1>` → `<h1>{t("title")}</h1>`）
  - 属性：`placeholder` / `title` / `aria-label` / `alt` / `aria-description`
  - 浏览器 API：`window.alert(...)` / `window.confirm(...)` / `window.prompt(...)`
  - Toast / Notification 消息内容
  - 表单校验文案（走 [web-form-convention](../web-form-convention/SKILL.md) 的 `useSchema`）
  - 错误兜底文案（`err instanceof Error ? err.message : t("xxxFailed")`）
- [ ] 同时在 `messages/zh.json` **和** `messages/en.json` 添加对应 key（**两边都加**，少一边就是 ICU MISSING_MESSAGE 报错）
- [ ] key 命名：嵌套 namespace + camelCase，例如 `session.deleteFailedNetwork`、`schedule.title`，避免重复或全局裸 key
- [ ] 自检脚本通过（见下方"自检命令"）

> aria-label 也必须翻译 —— 它对屏幕阅读器是关键交互文本，不是"不会被看到的内部串"。

---

## 标准写法

### 客户端组件（"use client"）

```tsx
"use client";

import { useTranslations } from "next-intl";

export default function SchedulePage() {
  const t = useTranslations("schedule");
  return (
    <div className="flex h-full items-center justify-center text-muted-foreground">
      {t("title")}
    </div>
  );
}
```

`messages/zh.json`:
```json
{
  "schedule": { "title": "计划任务" }
}
```

`messages/en.json`:
```json
{
  "schedule": { "title": "Schedule" }
}
```

### 服务端组件（Next.js App Router）

```tsx
import { getTranslations } from "next-intl/server";

export default async function Page() {
  const t = await getTranslations("home");
  return <h1>{t("title")}</h1>;
}
```

### 浏览器 API / 命令式调用

```tsx
const t = useTranslations("session");
// ❌ window.alert("消息已开始处理，无法删除");
// ✅
window.alert(t("alertCannotDeleteWhileProcessing"));
if (!window.confirm(t("confirmOverwriteDraft"))) return;
```

### 错误兜底

```tsx
form.setError("root", {
  message: err instanceof Error ? err.message : t("registerFailed"),
});
```

---

## 反模式（评审会被打回）

- ❌ JSX 文本里写中文 / 英文裸串：`<h1>计划任务</h1>`
- ❌ `aria-label="编辑"` / `placeholder="请输入..."` / `title="复制"`
- ❌ `window.alert("...")` / `window.confirm("...")` 用裸串
- ❌ 把翻译 key 写成 "用户名不能为空" 这种「key 就是文案」的伪 i18n —— key 必须是语义 ID（camelCase 英文）
- ❌ 只更新 `zh.json` 没动 `en.json`（或反过来）—— 切语言会跑出 MISSING_MESSAGE 警告 + 渲染 key 字面量
- ❌ 在 utils / hooks / 非组件函数里用 `useTranslations`（违反 React Hook 规则）。要在调用方拿到 `t` 再传进来，或者 hook 内部用 `useTranslations` 并暴露派生值

---

## 自检命令（commit 前必跑）

扫描裸字符串（中文 / aria-label / alert / confirm / placeholder）：

```bash
# web-agent
rg -nE '(window\.(alert|confirm|prompt)\(\"|aria-label=\"[^"]*[^"{][^"]*\"|placeholder=\"[一-鿿]|title=\"[一-鿿]|>[\s]*[一-鿿]+[\s]*<)' \
  apps/web-agent/src/app apps/web-agent/src/components

# web-main
rg -nE '(window\.(alert|confirm|prompt)\(\"|aria-label=\"[^"]*[^"{][^"]*\"|placeholder=\"[一-鿿]|title=\"[一-鿿]|>[\s]*[一-鿿]+[\s]*<)' \
  apps/web-main/src/app apps/web-main/src/components
```

预期：**0 命中**。命中的位置需要全部走 `t("...")` 改造。

校验 zh / en key 对齐（防止只加一边）：

```bash
diff <(jq -S 'paths(scalars) | join(".")' apps/web-agent/messages/zh.json | sort) \
     <(jq -S 'paths(scalars) | join(".")' apps/web-agent/messages/en.json | sort)
```

预期：**无 diff**。

---

## 当前已知欠账（历史代码，遇到再补）

新代码绝不能再增加这类问题，历史欠账触碰相关文件时一并补齐：

- `apps/web-agent/src/app/session/page.tsx` — 4 处 `window.alert/confirm` + 1 个 `aria-label="滚动到底部"`
- `apps/web-agent/src/app/schedule/page.tsx` — 整页一句 `计划任务` 裸串
- `apps/web-agent/src/components/session/message-list.tsx` — `aria-label="正在生成回复"`
- `apps/web-agent/src/components/session/pending-list.tsx` — `aria-label="编辑" / "删除"`

---

## 与其他规范的关系

- 表单 / 校验文案：见 [web-form-convention](../web-form-convention/SKILL.md) 第 2、4 节（`useSchema` + `useTranslations`）
- 后端 API 错误码 i18n：走 `@meshbot/common` 的 `I18nService` + 错误码 namespace，不在本 skill 范围内
