---
description: Controller 瘦身规范 — Controller 只做请求接收与响应，业务逻辑必须下沉到 Service
globs:
  - apps/server-*/src/**/*.controller.ts
  - libs/*/src/**/*.controller.ts
alwaysApply: false
---

# Controller 瘦身规范

Controller 是 HTTP 层的薄代理，**只负责接收请求、调用 Service、返回结果**。所有业务逻辑（含权限判断、数据查找、条件分支、错误抛出）必须下沉到 Service 层。

## Controller 中允许做的事

- 提取请求参数（`@Body`、`@Param`、`@Query`、`@Req`）
- 组装 Service 调用所需的入参（如 `req.ip`、`req.headers["user-agent"]`）
- 调用 Service 方法
- 直接返回 Service 的结果

## Controller 中禁止做的事

- ❌ 查询数据库 / 调用其他 Service 进行业务判断
- ❌ `if / else` 业务分支（如判断用户是否有组织、资源是否存在）
- ❌ 手动抛出 `AppError`（业务校验失败应由 Service 抛出）
- ❌ 编排多个 Service 调用的先后顺序和错误回滚
- ❌ 包含 try/catch 处理业务异常（全局 `ErrorsFilter` 兜底）

## 示例

```ts
// ❌ 错误：Controller 包含业务逻辑
@Post("email/login")
async emailLogin(@Body() body: EmailLoginDto, @Req() req: Request) {
  await this.mailCodeService.verify(body.email, MailCodeAction.LOGIN, body.code);
  const { user, isNew } = await this.accountService.findOrCreateUserByEmail(body.email);
  let orgId: string | null = null;
  if (isNew) {
    const org = await this.organizationService.createOrgForNewUser(user.id, user.name);
    orgId = org.id;
  } else {
    // ... 更多分支 ...
  }
  await this.accountService.invalidateUserSessions(user.id);
  const session = await this.accountService.createSession({ userId: user.id, orgId, ... });
  return { token: session.token };
}

// ✅ 正确：Controller 只做转发
@Post("email/login")
async emailLogin(@Body() body: EmailLoginDto, @Req() req: Request) {
  return this.accountService.loginByEmail(body.email, body.code, {
    ip: req.ip ?? null,
    userAgent: req.headers["user-agent"] ?? null,
  });
}
```

```ts
// ❌ 错误：Controller 中做权限判断
@Post("channels")
async create(@Req() req: Request, @Body() body: CreateChannelDto) {
  const user = req.user!;
  if (!user.orgId) {
    throw new AppError(ErrorCode.FORBIDDEN, "用户尚未加入任何组织");
  }
  const hired = await this.hiredAgentService.findByOrgIdAndAgentId(user.orgId, body.agentId);
  if (!hired?.instanceId) {
    throw new AppError(ErrorCode.NOT_FOUND, "未雇佣该 Agent");
  }
  return this.channelService.create(hired.instanceId, body);
}

// ✅ 正确：权限判断下沉到 Service
@Post("channels")
async create(@Req() req: Request, @Body() body: CreateChannelDto) {
  return this.channelService.createForUser(req.user!.id, req.user!.orgId, body);
}
```

## 例外

以下场景允许在 Controller 中保留少量逻辑：

- **`@Res()` 手动响应**（如 OAuth redirect）— 需要直接操作 `response` 对象
- **流式响应**（SSE / Stream）— 需要手动写入 response
