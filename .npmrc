node-linker=hoisted

# 关掉 pnpm v10+ 默认的 "脚本执行前自动安装" 行为。
# 在 monorepo + electron-builder 这类多层嵌套 pnpm exec 的场景下，
# 这个特性会在 stage / before-build 过程中误判 node_modules 状态，
# 错把 prepare/husky 等 devDep 当成 production 模式跳过，导致 prepare 失败。
verify-deps-before-run=warn
