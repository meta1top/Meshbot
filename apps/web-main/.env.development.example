# 【可选覆盖】开发期 web-main（Next dev :3002）访问的 server-main 地址。
# dev 默认已由 next.config.ts 注入 http://localhost:3200，无需本文件即可开箱即用。
# 仅当要指向别的后端时，cp 本文件为 .env.development（已 gitignore）并改值。
NEXT_PUBLIC_SERVER_MAIN_URL=http://localhost:3200
