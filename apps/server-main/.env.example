# server-main 环境变量样例。
#
# 注意：runtime 只读 `.env`（不再读 .env.development）。复制本文件为 `.env` 后按需填写。
#
# 配置体系（loadAppConfig）：
#   - 数据库 / JWT / Redis / 邮件等业务配置 → `conf/application.yml`（已提交，本地开发开箱即用）
#   - 个人覆盖 → `conf/application.local.yml`（gitignore，不提交）
#   - 部署环境 → 配置 NACOS_SERVER_ADDR 后从 Nacos 拉取嵌套配置，本地 YAML 不再被读取
#   - `.env` 只放 Nacos 引导变量 + 进程级 env（如 MESHBOT_NODE_ID）
#
# 启动本地 Postgres / Redis：pnpm dev:db:up

# ---- Nacos 引导变量（全部可选；不设 NACOS_SERVER_ADDR 则走本地 conf/application.yml）----
# Nacos 里该放什么内容见 conf/nacos-example.yml（完整配置样例，含生产注意事项）。
# NACOS_SERVER_ADDR=nacos.example.com:8848
# NACOS_NAMESPACE=meshbot
# NACOS_GROUP=DEFAULT_GROUP
# NACOS_DATA_ID=meshbot-server-main.yaml   # 默认值即此，不改可省略
# NACOS_USERNAME=nacos
# NACOS_PASSWORD=nacos

# ---- Snowflake 节点 ID（多副本部署必配；范围 0-1023，每副本唯一）----
# 配置了 Redis（多副本信号）但未设置 MESHBOT_NODE_ID 时启动会 fail-fast。
# MESHBOT_NODE_ID=0
